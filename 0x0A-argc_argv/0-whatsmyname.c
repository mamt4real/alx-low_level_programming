#include<stdio.h>

/**
 * main - Entry point
 * @argc: number of arguments
 * @argv: arguments array
 * Return: 0 always
 */
int main(int argc, char **argv)
{
	/* print the programme name */
	printf("%s\n", argv[0]);
	return (0);
}

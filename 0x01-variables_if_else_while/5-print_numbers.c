#include<stdio.h>

/**
 * main - Entry function
 * Deacription: prints Digits
 * Return: 0 indicating success
 */
int main(void)
{
	int c;

	for (c = 0; c < 10; c++)
		printf("%d", c);
	putchar('\n');
	return (0);
}
